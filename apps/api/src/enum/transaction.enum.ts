export enum TransactionType {
  PRE_DEDUCT = "PRE_DEDUCT",
  SETTLE = "SETTLE",
  ROLLBACK = "ROLLBACK",
  TOPUP = "TOPUP",
}

export enum TransactionStatus {
  PENDING = "PENDING",
  SUCCESS = "SUCCESS",
  FAILED = "FAILED",
}

