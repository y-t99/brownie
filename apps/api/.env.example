# Server Configuration
PORT=3001
NODE_ENV=development

# CORS (comma-separated, e.g. http://localhost:3002)
# If unset, reflects request origin.
# CORS_ORIGINS=http://localhost:3002

# Session cookie (used by Developer Console)
# AUTH_COOKIE_NAME=brownie_session
# AUTH_COOKIE_DOMAIN=
# AUTH_COOKIE_SAMESITE=lax

# Database Configuration (if needed)
# DATABASE_URL=postgresql://username:password@localhost:5432/brownie_api

# JWT Configuration (if needed)
# JWT_SECRET=your-jwt-secret-key
# JWT_EXPIRES_IN=7d

# Google Generative AI Configuration
GOOGLE_GENERATIVE_AI_API_KEY=your-google-api-key

# S3-Compatible Object Storage Configuration
S3_ENDPOINT=https://s3.amazonaws.com
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key
S3_BUCKET=your-bucket-name
S3_FORCE_PATH_STYLE=true
